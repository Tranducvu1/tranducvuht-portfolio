version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 18.19.0 || nvm install 18.19.0
    build:
      commands:
        - yarn install --frozen-lockfile
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'