version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || nvm install 20
    build:
      commands:
        - npm ci
        - npm run build
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
