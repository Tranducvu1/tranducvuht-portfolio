version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20 || nvm install 20
    build:
      commands:
        - yarn install --frozen-lockfile
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'