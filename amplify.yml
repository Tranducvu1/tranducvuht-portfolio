version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 22.11.0 || nvm install 22.11.0
    build:
      commands:
        - yarn install --frozen-lockfile
        - yarn build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'